---
import Model3D from "./Model3D.astro";

interface Item {
  src: string;
  alt: string;
  caption?: string;
  color?: string;
}

interface Props {
  items: [Item, Item];
  height?: string;
}

const { items, height = "440px" } = Astro.props;
---

{/* Two STL viewers side by side on desktop; only the first on mobile. */}
<div
  class="not-prose my-8 grid grid-cols-1 md:grid-cols-2 gap-4 [&_figure]:my-0"
>
  <Model3D
    src={items[0].src}
    alt={items[0].alt}
    caption={items[0].caption}
    color={items[0].color}
    height={height}
  />
  <div class="hidden md:block">
    <Model3D
      src={items[1].src}
      alt={items[1].alt}
      caption={items[1].caption}
      color={items[1].color}
      height={height}
    />
  </div>
</div>
